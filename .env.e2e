# E2E environment — no real OpenRouter key needed.
# The compose.e2e.yaml overlay provides the local embedding stub.
#
# Usage:
#   podman compose -f compose.yaml -f compose.e2e.yaml --env-file .env.e2e up -d --build

KNOWLEDGE_API_INTERNAL_TOKEN=local-dev-token
