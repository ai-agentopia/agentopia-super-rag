# Dependency baseline mirrored from agentopia-protocol/knowledge-api requirements.txt
# as of freeze commit (2026-04-09). Uses minimum-version constraints (>= ranges),
# not fully pinned. Phase 2 extraction will validate these against the running service.
# See docs/migration.md for extraction plan.

fastapi>=0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.11.3
httpx>=0.27.0
PyPDF2==3.0.1
beautifulsoup4==4.14.3
python-multipart==0.0.22
kubernetes>=29.0.0
psycopg[binary]>=3.1.0
openai>=1.0.0
qdrant-client>=1.17.0

# Test / dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
